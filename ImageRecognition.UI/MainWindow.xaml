<Window x:Class="ImageRecognition.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Image Recognition (kNN + KDTree)" Height="640" Width="980"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Class name:"/>
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="ClassNameTextBox" Margin="4" Text="digit_0"/>
        <Button Grid.Row="0" Grid.Column="2" Margin="4" Content="Initialize Database" Click="InitializeDatabaseButton_Click"/>

        <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Text="Image path:"/>
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="ImagePathTextBox" Margin="4"/>
        <Button Grid.Row="1" Grid.Column="2" Margin="4" Content="Browse Image..." Click="BrowseImageButton_Click"/>

        <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="k:"/>
        <TextBox Grid.Row="2" Grid.Column="1" x:Name="KTextBox" Margin="4" Text="3"/>
        <Button Grid.Row="2" Grid.Column="2" Margin="4" Content="Add Training Image" Click="AddTrainingImageButton_Click"/>

        <TextBlock Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Text="Model name:"/>
        <TextBox Grid.Row="3" Grid.Column="1" x:Name="ModelNameTextBox" Margin="4" Text="kNN_coursework_model"/>
        <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Vertical" Margin="4">
            <Button Margin="0,0,0,6" Content="Train Model" Click="TrainModelButton_Click"/>
            <Button Margin="0,0,0,6" Content="Classify (KDTree)" Click="ClassifyKdTreeButton_Click"/>
            <Button Margin="0,0,0,6" Content="Classify (Linear)" Click="ClassifyLinearButton_Click"/>
            <Button Content="Run Benchmark" Click="BenchmarkButton_Click"/>
        </StackPanel>

        <TextBox Grid.Row="4" Grid.ColumnSpan="3"
                 x:Name="LogTextBox"
                 Margin="4"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"/>
    </Grid>
</Window>
